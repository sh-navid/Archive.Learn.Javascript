# JQuery
## Other

- Data
    - `.data()`
    - `.hasData()`
    - `.removeData()`
- Type
    - `.type()`
    - `.isXMLDoc()`
    - `.isEmptyObject()`
    - `.isFunction()`
    - `.isNumeric()`
    - `.isPlainObject()`
    - `.isWindow()`


- `.extend()`
- `.merge()`

- `.grep()`
- `.noop()`
- `.now()`
- `.trim()`
- `.unique()`
- `.uniqueSort()`

- `$.cssNumber`
- `$.cssHooks`


- `.escapeSelector()`