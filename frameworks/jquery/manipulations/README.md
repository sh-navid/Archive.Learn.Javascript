# JQuery
## Manipulations
- `.before()`
- `.after()`
- `.insertBefore()`
- `.insertAfter()`
- `.append()`
- `.appendTo()`
- `.prepend()`
- `.prependTo()`
- `.clone()`
- `.detach()`
- Delete
    - `.empty()`
    - `.remove()`
- Replace
    - `.replaceWith()`
    - `.replaceAll()`
- Wrap
    - `.unwrap()`
    - `.wrap()`
    - `.wrapAll()`
    - `.wrapInner()`