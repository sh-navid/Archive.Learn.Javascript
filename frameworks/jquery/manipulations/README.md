# JQuery
## Manipulations
- `.before()`
- `.after()`
- `.insertBefore()`
- `.insertAfter()`
- `.append()`
- `.appendTo()`
- `.prepend()`
- `.prependTo()`
- `.clone()`
- `.detach()`
- `.empty()`
- `.remove()`
- Replace
    - `.replaceWith()`
    - `.replaceAll()`
- Wrap
    - `.unwrap()`
    - `.wrap()`
    - `.wrapAll()`
    - `.wrapInner()`