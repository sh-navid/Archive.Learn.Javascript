# JQuery
## Values
- Dimension
    - `.width()`
    - `.height()`
    - `.innerWidth()`
        - _Include padding_
    - `.innerHeight()`
        - _Include padding_
    - `.outerWidth()`
        - _Include padding + border + margin_
    - `.outerHeight()`
        - _Include padding + border + margin_
- Coordinates
    - `.offset()`
    - `.offsetParent()`
    - `.position()`
- Scroll
    - `.scrollLeft()`
    - `.scrollTop()`